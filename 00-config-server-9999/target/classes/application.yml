server:
  port: 9999
spring:
  cloud:
    config:
      server:
        git:
          #远程库地址
          uri: https://github.com/shenmeng1990/springcloud_config.git
          #uri: git@github.com:shenmeng1990/springconfig_server.git
          #指定获取到git远程库连接的超时时限，默认为5S
          timeout: 5
          #指定要操作的远程库的分支
          default-label: master
          username: shenmeng1990
          password: inspur19881227
